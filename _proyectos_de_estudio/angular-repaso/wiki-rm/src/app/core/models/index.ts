/*
 * MODELS INDEX - Exportaciones de interfaces y tipos
 *
 * Este archivo centraliza todas las definiciones de tipos, interfaces y enums
 * que son utilizados globalmente en la aplicación.
 *
 * CATEGORÍAS DE MODELOS:
 *
 * 1. API MODELS
 *    - Interfaces que definen la estructura de respuestas de APIs
 *    - Tipos para requests y responses
 *    - Modelos de entidades del dominio
 *
 * 2. UI MODELS
 *    - Interfaces para componentes y estado de la UI
 *    - Tipos para formularios y validaciones
 *    - Estados de carga y error
 *
 * 3. CONFIGURATION MODELS
 *    - Tipos para configuraciones de la app
 *    - Interfaces para settings y preferencias
 *    - Constantes tipadas
 *
 * 4. UTILITY TYPES
 *    - Tipos helpers y genéricos
 *    - Union types y mapped types
 *    - Tipos computados y condicionales
 *
 * CONVENCIONES DE NOMENCLATURA:
 * - Interfaces: PascalCase (User, ApiResponse)
 * - Types: PascalCase (UserRole, Status)
 * - Enums: PascalCase (UserStatus, ApiEndpoint)
 * - Sufijos: Interface, Type, Enum (opcional pero recomendado)
 */

// TODO: Aquí se exportarán los modelos cuando se creen
// export * from './api.interface';
// export * from './user.interface';
// export * from './common.types';

// Por ahora, exportamos un placeholder
export const MODELS_INDEX = 'Models will be exported here';
